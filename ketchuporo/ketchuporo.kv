#:kivy 1.8.0

<Ketchuporo>:
    Label:
        font_size: 70
        center_x: root.width / 2
        top: root.top - 50
        text: root.model.timer_label
    Label:
        font_size: 70
        center_x: root.width / 2
        top: root.top - 200
        text: str(root.model.pomodoros_counter)
